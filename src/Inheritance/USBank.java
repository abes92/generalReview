package Inheritance;

public interface USBank {
    int min=200;
    public void credit();
    public void debit();
    //only method declaration no method body
    //only method prototype
    //in interface we can declare variables,they are by default static in nature
    //variable values will not be change
    // no static method and no main method
    //we can not create an object of interface
    //interface is abstract in nature
}
